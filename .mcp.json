{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "nix-shell",
      "args": ["--run", "npx -y @playwright/mcp --browser chromium --user-data-dir .cache/playwright-mcp-profile"]
    }
  }
}
